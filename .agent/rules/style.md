---
trigger: always_on
---

Use Object Oriented (classes), with capital path and file (/Thing/Thing.js).  Use PascalCase for classes, when appropriate.

Use snake_case, not camelCase, for vars, methods, args, but prefer short, single words, to avoid underscores (_), when possible.